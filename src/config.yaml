---
default:
  environments:
    - "production"

  config:
    database:
      server: "${env:DB_SERVER}"
      username: "${env:DB_USERNAME}"
      password: "${env:DB_PASSWORD}"
      database: "${env:DB_DATABASE}"
    baby:
      name: "Babiemans"
      conception_date: "2022-01-30"
